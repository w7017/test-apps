import React from 'react';

const PreparationEquipement = () => (
  <div className="p-8">
    <h2 className="text-2xl font-bold mb-6">Préparation équipement</h2>
    <div>Pré-configuration des fiches équipement avant saisie en base. (À implémenter)</div>
  </div>
);

export default PreparationEquipement; 