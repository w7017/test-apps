import React from 'react';

const OPLOPR = () => (
  <div className="p-8">
    <h2 className="text-2xl font-bold mb-6">Track OPL / OPR</h2>
    <div>Gestion des réserves en fin de chantier, workflow, statuts, notifications, génération de rapports. (À implémenter)</div>
  </div>
);

export default OPLOPR; 